[gcode_macro _HOME_X]
gcode:
    # Always use consistent run_current on A/B steppers during sensorless homing
    {% set RUN_CURRENT_X = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set RUN_CURRENT_Y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    # Bumped from 0.7 -- gives StallGuard more margin between "normal
    # accel load" and "actual stall" so ordinary ramp-up current draw is
    # less likely to read as a false stall.
    {% set HOME_CURRENT = 0.85 %}
    # [printer] max_accel (10000) is too aggressive for a homing move: the
    # current spike during that initial ramp can false-trigger StallGuard
    # before the toolhead ever reaches the real endstop. Cap accel just for
    # the homing move, restore the printer's real max_accel right after.
    {% set HOME_ACCEL = 1000 %}
    {% set ORIGINAL_ACCEL = printer.toolhead.max_accel|float %}
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT}
    SAVE_GCODE_STATE NAME=STATE_HOME_X

    # Wait 2s before homing (lets StallGuard settle from any prior motion)
    G4 P2000
    SET_VELOCITY_LIMIT ACCEL={HOME_ACCEL}
    # Home
    G28 X
    SET_VELOCITY_LIMIT ACCEL={ORIGINAL_ACCEL}
    # Move away
    G91
    G1 X-5 F1200

    # Wait just a second… (give StallGuard registers time to clear)
    G4 P500
    # Set current during print
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}
    RESTORE_GCODE_STATE NAME=STATE_HOME_X

[gcode_macro _HOME_Y]
gcode:
    # Set current for sensorless homing
    {% set RUN_CURRENT_X = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set RUN_CURRENT_Y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    # Bumped from 0.7 -- gives StallGuard more margin between "normal
    # accel load" and "actual stall" so ordinary ramp-up current draw is
    # less likely to read as a false stall.
    {% set HOME_CURRENT = 0.85 %}
    # [printer] max_accel (10000) is too aggressive for a homing move: the
    # current spike during that initial ramp can false-trigger StallGuard
    # before the toolhead ever reaches the real endstop. Cap accel just for
    # the homing move, restore the printer's real max_accel right after.
    {% set HOME_ACCEL = 1000 %}
    {% set ORIGINAL_ACCEL = printer.toolhead.max_accel|float %}
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT}
    SAVE_GCODE_STATE NAME=STATE_HOME_Y

    # Wait 2s before homing (lets StallGuard settle from any prior motion)
    G4 P2000
    SET_VELOCITY_LIMIT ACCEL={HOME_ACCEL}
    # Home
    G28 Y
    SET_VELOCITY_LIMIT ACCEL={ORIGINAL_ACCEL}
    # Move away
    G91
    G1 Y-5 F1200

    # Wait just a second… (give StallGuard registers time to clear)
    G4 P500
    # Set current during print
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}
    RESTORE_GCODE_STATE NAME=STATE_HOME_Y

[homing_override]
axes: xyz
set_position_z: 0
gcode:
    {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}
    SAVE_GCODE_STATE NAME=STATE_HOME_OVERRIDE

    # Home Z first, to avoid any situation where the nozzle might scrape the bed
    # (no dwell needed here - Z uses a physical endstop, not StallGuard)
    {% if home_all or 'Z' in params %}
        G28 Z
    {% endif %}

    {% if home_all or 'X' in params %}
        _HOME_X
    {% endif %}

    {% if home_all or 'Y' in params %}
        _HOME_Y
    {% endif %}

    G90

    # Move to bed center only if homing all axes or explicitly commanded for X and Y axes
    {% if home_all or ('X' in params and 'Y' in params) %}
      G0 X60 Y60 Z20 F3600 # move to bed center
    {% endif %}
    RESTORE_GCODE_STATE NAME=STATE_HOME_OVERRIDE



